# Scrabble Light -- site espagnol (wordcheckr.es).
# Perimetre de l'agent seo-registry (CLAUDE.md) -- pas encore construit pour ce site
# (registre SEO espagnol hors perimetre du lot core lookup site, storage/seo_es.sqlite
# n'existe pas). En son absence, App\Seo\Registry retombe sur noindex,follow par defaut
# sur chaque page (D-005) -- ce fichier ne fait qu'autoriser le crawl, il ne decide rien.
#
# Aucune ligne Sitemap: tant qu'aucun sitemap espagnol reel n'existe (audit round 2, C-2 --
# les 36 fichiers sitemaps francais herites de la copie initiale FR->ES, qui annoncaient
# https://www.wordcheckr.fr, ont ete supprimes ici plutot que laisses a servir un domaine
# et un schema d'URL qui ne sont plus les siens).

User-agent: *
Allow: /
